# gdal/ogr python bindings
python-dateutil
psycopg2
numpy

# Needed for testing.
mock

# Just for documentation generation
sphinx
sphinx-autobuild

# Needed for testing & if tegola import handler is enabled.
toml
